pragma solidity ^0.5.0;

contract TodoList {
    uint public taskCount = 0;  // "state" variable, like Class variable, written to the blockchain.
                                // Public: we can read the value from the smart contract, like an implicit get().
}
